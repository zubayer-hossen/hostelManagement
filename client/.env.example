# In development the Vite dev server proxies /api to http://localhost:5000 (see vite.config.js).
# In production set the full backend URL, e.g. https://api.your-domain.com/api/v1
VITE_API_URL=/api/v1
